/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ['images.unsplash.com'], // Agrega aquí los dominios de tus imágenes permitidos
  },};


export default nextConfig;
